var periscope = require('../periscope.js');

describe('Periscope parsing API', function () {
  var periscope;

  describe('Parse method', function() {
    beforeEach(function () {
    });

    it('should accept .md or .markdown files for parsing', function () {
      
    });

    it('should accept .html or .htm files for parsing', function () {
      
    });
  });
});